

这是一个简单的正则解释器
使用龙书上先用预测分析加递归下降的方法生成语法树，然后求first，last，follow集并求得DFA，但并没有对DFA做最小化
对龙书上的正则表达式做了简单的扩展
增加了对 [a-z1-9], x{8}, x{0-7}, x+, x? 几种语法的支持

当作是学习龙书的小练习，以后可能会使用先生成NFA，再转换成DFA的方法重做一遍，并加上匿名捕获等扩展功能
由于作者智力低下，封装做得比较差，将来重写的时候再改进吧